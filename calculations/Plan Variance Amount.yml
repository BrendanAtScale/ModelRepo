unique_name: Plan Variance Amount
object_type: metric_calc
label: Plan Variance Amount
expression: >-
  0 /*If(SELECTEDVALUE('Line Format'[LF Level 3
  Code])="952500.0002",CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Plan"))-CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Actual")),If(SELECTEDVALUE('Line
  Format'[LF Level 3 Code])="953000.0002",CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Plan"))-CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Actual")),If(SELECTEDVALUE('Line
  Format'[LF Level 2 Code])="446000.0002",CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Plan"))-CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Actual")),If(SELECTEDVALUE('Line
  Format'[LF Level 5 Code])="363000.0002",CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Actual"))-CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Plan")), IF(SELECTEDVALUE('Line
  Format'[LF Level 4 Code])="167000.0002",CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Plan"))-CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Actual")),CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Actual"))-CALCULATE([Standard
  Amount],FILTER(Scenario,Scenario[Scenario]="Plan"))))))) TODO: Update with
  valid MDX expression*/
format: '#,##0;(#,##0);;'
