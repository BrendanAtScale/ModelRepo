unique_name: YTD Dates
object_type: metric_calc
label: YTD Dates
expression: >-
  0 /*VAR EndDate = TODAY () VAR StartDate = DATE ( YEAR ( TODAY () ), 1, 1 )
  RETURN IF ( MAX ( 'Accounting Date'[Accounting Date] ) >= StartDate && MAX (
  'Accounting Date'[Accounting Date] ) <= EndDate, TRUE (), FALSE () ) TODO:
  Update with valid MDX expression*/
folder: Other Calculated Amts
is_hidden: true
