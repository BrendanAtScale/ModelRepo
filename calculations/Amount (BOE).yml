unique_name: Amount (BOE)
object_type: metric_calc
label: Amount (BOE)
expression: >-
  0 /*CALCULATE ( IF ( HASONEVALUE ( 'COA'[COA Name] ), IF ( CONTAINSSTRING (
  MAX ( 'COA'[COA Name] ), "BTU" ), 0, DIVIDE ( [Amount (vol)], IF (
  CONTAINSSTRING ( MAX ( 'COA'[COA Name] ), "GAS" ), 6, 1 ) ) ) ), KEEPFILTERS (
  'Line Format' ) ) TODO: Update with valid MDX expression*/
folder: Other Calculated Amts
format: '#,##0;(#,##0);;'
